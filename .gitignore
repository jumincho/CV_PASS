# Gradle
.gradle/
/build
app/build/
**/build/

# Android Studio / IntelliJ
*.iml
.idea/

# Local config & secrets
local.properties
/local.properties
# app/google-services.json is intentionally committed as a placeholder so the
# project configures out of the box (see README); swap in your own to run it.

# OS / misc
.DS_Store
/captures
.externalNativeBuild
.cxx
